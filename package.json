{
  "name": "payway-checkout-nodejs",
  "version": "1.0.0",
  "description": "ABA PayWay Checkout Integration in Node.js",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "simple": "node -e \"require('dotenv').config(); const server = require('./server.js');\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["payway", "aba", "payment", "checkout", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9",
    "dotenv": "^16.3.1",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}